import { motion } from "framer-motion";
import React from "react";
type Props = {};

const TestComponent = ({}: Props) => {
  return (
    <>
      <motion.div animate={} transition={{ delayChildren: 1 }}>
        asdjkashdkjashd
      </motion.div>
      ;
    </>
  );
};

export default TestComponent;
